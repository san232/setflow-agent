# Leave both empty for Demo Mode. Set your own API key locally only.
OPENAI_API_KEY=
OPENAI_MODEL=
# Relative paths are resolved from the project directory.
SETFLOW_DB_PATH=data/setflow.db
SETFLOW_DEMO_MODE=false
SETFLOW_MAX_TOOL_ROUNDS=6
SETFLOW_API_TIMEOUT=30
SETFLOW_AUTO_SEED=false
SETFLOW_PUBLIC_DEMO=false
